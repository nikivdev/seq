# Rise Agents

Add .tsx agent specs here. Use the `use rise` directive so they compile.
Example:

```tsx
use rise

export const Agent = {
  name: "global-helper",
  description: "Global helper agent",
}
```
