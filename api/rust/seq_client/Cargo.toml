[package]
name = "seq_client"
version = "0.1.0"
edition = "2021"
description = "Rust client for seqd Agent RPC v1 over Unix sockets"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"

